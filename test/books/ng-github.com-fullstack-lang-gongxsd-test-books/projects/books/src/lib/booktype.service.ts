// generated by ng_file_service_ts
import { Injectable, Component, Inject } from '@angular/core';
import { HttpClientModule, HttpParams } from '@angular/common/http';
import { HttpClient, HttpHeaders } from '@angular/common/http';
import { DOCUMENT, Location } from '@angular/common'

/*
 * Behavior subject
 */
import { BehaviorSubject } from 'rxjs'
import { Observable, of } from 'rxjs'
import { catchError, map, tap } from 'rxjs/operators'

import { BookTypeAPI } from './booktype-api'
import { BookType, CopyBookTypeToBookTypeAPI } from './booktype'

import { FrontRepo, FrontRepoService } from './front-repo.service';

// insertion point for imports
import { CreditAPI } from './credit-api'

@Injectable({
  providedIn: 'root'
})
export class BookTypeService {

  // Kamar Raïmo: Adding a way to communicate between components that share information
  // so that they are notified of a change.
  BookTypeServiceChanged: BehaviorSubject<string> = new BehaviorSubject("");

  private booktypesUrl: string

  constructor(
    private http: HttpClient,
    @Inject(DOCUMENT) private document: Document
  ) {
    // path to the service share the same origin with the path to the document
    // get the origin in the URL to the document
    let origin = this.document.location.origin

    // if debugging with ng, replace 4200 with 8080
    origin = origin.replace("4200", "8080")

    // compute path to the service
    this.booktypesUrl = origin + '/api/github.com/fullstack-lang/gongxsd/test/books/go/v1/booktypes';
  }

  /** GET booktypes from the server */
  // gets is more robust to refactoring
  gets(GONG__StackPath: string, frontRepo: FrontRepo): Observable<BookTypeAPI[]> {
    return this.getBookTypes(GONG__StackPath, frontRepo)
  }
  getBookTypes(GONG__StackPath: string, frontRepo: FrontRepo): Observable<BookTypeAPI[]> {

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)

    return this.http.get<BookTypeAPI[]>(this.booktypesUrl, { params: params })
      .pipe(
        tap(),
        catchError(this.handleError<BookTypeAPI[]>('getBookTypes', []))
      );
  }

  /** GET booktype by id. Will 404 if id not found */
  // more robust API to refactoring
  get(id: number, GONG__StackPath: string, frontRepo: FrontRepo): Observable<BookTypeAPI> {
    return this.getBookType(id, GONG__StackPath, frontRepo)
  }
  getBookType(id: number, GONG__StackPath: string, frontRepo: FrontRepo): Observable<BookTypeAPI> {

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)

    const url = `${this.booktypesUrl}/${id}`;
    return this.http.get<BookTypeAPI>(url, { params: params }).pipe(
      // tap(_ => this.log(`fetched booktype id=${id}`)),
      catchError(this.handleError<BookTypeAPI>(`getBookType id=${id}`))
    );
  }

  // postFront copy booktype to a version with encoded pointers and post to the back
  postFront(booktype: BookType, GONG__StackPath: string): Observable<BookTypeAPI> {
    let booktypeAPI = new BookTypeAPI
    CopyBookTypeToBookTypeAPI(booktype, booktypeAPI)
    const id = typeof booktypeAPI === 'number' ? booktypeAPI : booktypeAPI.ID
    const url = `${this.booktypesUrl}/${id}`;
    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.post<BookTypeAPI>(url, booktypeAPI, httpOptions).pipe(
      tap(_ => {
      }),
      catchError(this.handleError<BookTypeAPI>('postBookType'))
    );
  }
  
  /** POST: add a new booktype to the server */
  post(booktypedb: BookTypeAPI, GONG__StackPath: string, frontRepo: FrontRepo): Observable<BookTypeAPI> {
    return this.postBookType(booktypedb, GONG__StackPath, frontRepo)
  }
  postBookType(booktypedb: BookTypeAPI, GONG__StackPath: string, frontRepo: FrontRepo): Observable<BookTypeAPI> {

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.post<BookTypeAPI>(this.booktypesUrl, booktypedb, httpOptions).pipe(
      tap(_ => {
        // this.log(`posted booktypedb id=${booktypedb.ID}`)
      }),
      catchError(this.handleError<BookTypeAPI>('postBookType'))
    );
  }

  /** DELETE: delete the booktypedb from the server */
  delete(booktypedb: BookTypeAPI | number, GONG__StackPath: string): Observable<BookTypeAPI> {
    return this.deleteBookType(booktypedb, GONG__StackPath)
  }
  deleteBookType(booktypedb: BookTypeAPI | number, GONG__StackPath: string): Observable<BookTypeAPI> {
    const id = typeof booktypedb === 'number' ? booktypedb : booktypedb.ID;
    const url = `${this.booktypesUrl}/${id}`;

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.delete<BookTypeAPI>(url, httpOptions).pipe(
      tap(_ => this.log(`deleted booktypedb id=${id}`)),
      catchError(this.handleError<BookTypeAPI>('deleteBookType'))
    );
  }

  // updateFront copy booktype to a version with encoded pointers and update to the back
  updateFront(booktype: BookType, GONG__StackPath: string): Observable<BookTypeAPI> {
    let booktypeAPI = new BookTypeAPI
    CopyBookTypeToBookTypeAPI(booktype, booktypeAPI)
    const id = typeof booktypeAPI === 'number' ? booktypeAPI : booktypeAPI.ID
    const url = `${this.booktypesUrl}/${id}`;
    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.put<BookTypeAPI>(url, booktypeAPI, httpOptions).pipe(
      tap(_ => {
      }),
      catchError(this.handleError<BookTypeAPI>('updateBookType'))
    );
  }

  /** PUT: update the booktypedb on the server */
  update(booktypedb: BookTypeAPI, GONG__StackPath: string, frontRepo: FrontRepo): Observable<BookTypeAPI> {
    return this.updateBookType(booktypedb, GONG__StackPath, frontRepo)
  }
  updateBookType(booktypedb: BookTypeAPI, GONG__StackPath: string, frontRepo: FrontRepo): Observable<BookTypeAPI> {
    const id = typeof booktypedb === 'number' ? booktypedb : booktypedb.ID;
    const url = `${this.booktypesUrl}/${id}`;


    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.put<BookTypeAPI>(url, booktypedb, httpOptions).pipe(
      tap(_ => {
        // this.log(`updated booktypedb id=${booktypedb.ID}`)
      }),
      catchError(this.handleError<BookTypeAPI>('updateBookType'))
    );
  }

  /**
   * Handle Http operation that failed.
   * Let the app continue.
   * @param operation - name of the operation that failed
   * @param result - optional value to return as the observable result
   */
  private handleError<T>(operation = 'operation in BookTypeService', result?: T) {
    return (error: any): Observable<T> => {

      // TODO: send the error to remote logging infrastructure
      console.error("BookTypeService" + error); // log to console instead

      // TODO: better job of transforming error for user consumption
      this.log(`${operation} failed: ${error.message}`);

      // Let the app keep running by returning an empty result.
      return of(result as T);
    };
  }

  private log(message: string) {
    console.log(message)
  }
}
