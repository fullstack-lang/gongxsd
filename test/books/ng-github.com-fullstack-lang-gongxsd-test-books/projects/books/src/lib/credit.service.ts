// generated by ng_file_service_ts
import { Injectable, Component, Inject } from '@angular/core';
import { HttpClientModule, HttpParams } from '@angular/common/http';
import { HttpClient, HttpHeaders } from '@angular/common/http';
import { DOCUMENT, Location } from '@angular/common'

/*
 * Behavior subject
 */
import { BehaviorSubject } from 'rxjs'
import { Observable, of } from 'rxjs'
import { catchError, map, tap } from 'rxjs/operators'

import { CreditAPI } from './credit-api'
import { Credit, CopyCreditToCreditAPI } from './credit'

import { FrontRepo, FrontRepoService } from './front-repo.service';

// insertion point for imports
import { LinkAPI } from './link-api'

@Injectable({
  providedIn: 'root'
})
export class CreditService {

  // Kamar Raïmo: Adding a way to communicate between components that share information
  // so that they are notified of a change.
  CreditServiceChanged: BehaviorSubject<string> = new BehaviorSubject("");

  private creditsUrl: string

  constructor(
    private http: HttpClient,
    @Inject(DOCUMENT) private document: Document
  ) {
    // path to the service share the same origin with the path to the document
    // get the origin in the URL to the document
    let origin = this.document.location.origin

    // if debugging with ng, replace 4200 with 8080
    origin = origin.replace("4200", "8080")

    // compute path to the service
    this.creditsUrl = origin + '/api/github.com/fullstack-lang/gongxsd/test/books/go/v1/credits';
  }

  /** GET credits from the server */
  // gets is more robust to refactoring
  gets(GONG__StackPath: string, frontRepo: FrontRepo): Observable<CreditAPI[]> {
    return this.getCredits(GONG__StackPath, frontRepo)
  }
  getCredits(GONG__StackPath: string, frontRepo: FrontRepo): Observable<CreditAPI[]> {

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)

    return this.http.get<CreditAPI[]>(this.creditsUrl, { params: params })
      .pipe(
        tap(),
        catchError(this.handleError<CreditAPI[]>('getCredits', []))
      );
  }

  /** GET credit by id. Will 404 if id not found */
  // more robust API to refactoring
  get(id: number, GONG__StackPath: string, frontRepo: FrontRepo): Observable<CreditAPI> {
    return this.getCredit(id, GONG__StackPath, frontRepo)
  }
  getCredit(id: number, GONG__StackPath: string, frontRepo: FrontRepo): Observable<CreditAPI> {

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)

    const url = `${this.creditsUrl}/${id}`;
    return this.http.get<CreditAPI>(url, { params: params }).pipe(
      // tap(_ => this.log(`fetched credit id=${id}`)),
      catchError(this.handleError<CreditAPI>(`getCredit id=${id}`))
    );
  }

  // postFront copy credit to a version with encoded pointers and post to the back
  postFront(credit: Credit, GONG__StackPath: string): Observable<CreditAPI> {
    let creditAPI = new CreditAPI
    CopyCreditToCreditAPI(credit, creditAPI)
    const id = typeof creditAPI === 'number' ? creditAPI : creditAPI.ID
    const url = `${this.creditsUrl}/${id}`;
    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.post<CreditAPI>(url, creditAPI, httpOptions).pipe(
      tap(_ => {
      }),
      catchError(this.handleError<CreditAPI>('postCredit'))
    );
  }
  
  /** POST: add a new credit to the server */
  post(creditdb: CreditAPI, GONG__StackPath: string, frontRepo: FrontRepo): Observable<CreditAPI> {
    return this.postCredit(creditdb, GONG__StackPath, frontRepo)
  }
  postCredit(creditdb: CreditAPI, GONG__StackPath: string, frontRepo: FrontRepo): Observable<CreditAPI> {

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.post<CreditAPI>(this.creditsUrl, creditdb, httpOptions).pipe(
      tap(_ => {
        // this.log(`posted creditdb id=${creditdb.ID}`)
      }),
      catchError(this.handleError<CreditAPI>('postCredit'))
    );
  }

  /** DELETE: delete the creditdb from the server */
  delete(creditdb: CreditAPI | number, GONG__StackPath: string): Observable<CreditAPI> {
    return this.deleteCredit(creditdb, GONG__StackPath)
  }
  deleteCredit(creditdb: CreditAPI | number, GONG__StackPath: string): Observable<CreditAPI> {
    const id = typeof creditdb === 'number' ? creditdb : creditdb.ID;
    const url = `${this.creditsUrl}/${id}`;

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.delete<CreditAPI>(url, httpOptions).pipe(
      tap(_ => this.log(`deleted creditdb id=${id}`)),
      catchError(this.handleError<CreditAPI>('deleteCredit'))
    );
  }

  // updateFront copy credit to a version with encoded pointers and update to the back
  updateFront(credit: Credit, GONG__StackPath: string): Observable<CreditAPI> {
    let creditAPI = new CreditAPI
    CopyCreditToCreditAPI(credit, creditAPI)
    const id = typeof creditAPI === 'number' ? creditAPI : creditAPI.ID
    const url = `${this.creditsUrl}/${id}`;
    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.put<CreditAPI>(url, creditAPI, httpOptions).pipe(
      tap(_ => {
      }),
      catchError(this.handleError<CreditAPI>('updateCredit'))
    );
  }

  /** PUT: update the creditdb on the server */
  update(creditdb: CreditAPI, GONG__StackPath: string, frontRepo: FrontRepo): Observable<CreditAPI> {
    return this.updateCredit(creditdb, GONG__StackPath, frontRepo)
  }
  updateCredit(creditdb: CreditAPI, GONG__StackPath: string, frontRepo: FrontRepo): Observable<CreditAPI> {
    const id = typeof creditdb === 'number' ? creditdb : creditdb.ID;
    const url = `${this.creditsUrl}/${id}`;


    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.put<CreditAPI>(url, creditdb, httpOptions).pipe(
      tap(_ => {
        // this.log(`updated creditdb id=${creditdb.ID}`)
      }),
      catchError(this.handleError<CreditAPI>('updateCredit'))
    );
  }

  /**
   * Handle Http operation that failed.
   * Let the app continue.
   * @param operation - name of the operation that failed
   * @param result - optional value to return as the observable result
   */
  private handleError<T>(operation = 'operation in CreditService', result?: T) {
    return (error: any): Observable<T> => {

      // TODO: send the error to remote logging infrastructure
      console.error("CreditService" + error); // log to console instead

      // TODO: better job of transforming error for user consumption
      this.log(`${operation} failed: ${error.message}`);

      // Let the app keep running by returning an empty result.
      return of(result as T);
    };
  }

  private log(message: string) {
    console.log(message)
  }
}
