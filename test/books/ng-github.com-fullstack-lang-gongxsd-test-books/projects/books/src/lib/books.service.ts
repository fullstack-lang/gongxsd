// generated by ng_file_service_ts
import { Injectable, Component, Inject } from '@angular/core';
import { HttpClientModule, HttpParams } from '@angular/common/http';
import { HttpClient, HttpHeaders } from '@angular/common/http';
import { DOCUMENT, Location } from '@angular/common'

/*
 * Behavior subject
 */
import { BehaviorSubject } from 'rxjs'
import { Observable, of } from 'rxjs'
import { catchError, map, tap } from 'rxjs/operators'

import { BooksAPI } from './books-api'
import { Books, CopyBooksToBooksAPI } from './books'

import { FrontRepo, FrontRepoService } from './front-repo.service';

// insertion point for imports
import { BookTypeAPI } from './booktype-api'

@Injectable({
  providedIn: 'root'
})
export class BooksService {

  // Kamar Raïmo: Adding a way to communicate between components that share information
  // so that they are notified of a change.
  BooksServiceChanged: BehaviorSubject<string> = new BehaviorSubject("");

  private bookssUrl: string

  constructor(
    private http: HttpClient,
    @Inject(DOCUMENT) private document: Document
  ) {
    // path to the service share the same origin with the path to the document
    // get the origin in the URL to the document
    let origin = this.document.location.origin

    // if debugging with ng, replace 4200 with 8080
    origin = origin.replace("4200", "8080")

    // compute path to the service
    this.bookssUrl = origin + '/api/github.com/fullstack-lang/gongxsd/test/books/go/v1/bookss';
  }

  /** GET bookss from the server */
  // gets is more robust to refactoring
  gets(GONG__StackPath: string, frontRepo: FrontRepo): Observable<BooksAPI[]> {
    return this.getBookss(GONG__StackPath, frontRepo)
  }
  getBookss(GONG__StackPath: string, frontRepo: FrontRepo): Observable<BooksAPI[]> {

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)

    return this.http.get<BooksAPI[]>(this.bookssUrl, { params: params })
      .pipe(
        tap(),
        catchError(this.handleError<BooksAPI[]>('getBookss', []))
      );
  }

  /** GET books by id. Will 404 if id not found */
  // more robust API to refactoring
  get(id: number, GONG__StackPath: string, frontRepo: FrontRepo): Observable<BooksAPI> {
    return this.getBooks(id, GONG__StackPath, frontRepo)
  }
  getBooks(id: number, GONG__StackPath: string, frontRepo: FrontRepo): Observable<BooksAPI> {

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)

    const url = `${this.bookssUrl}/${id}`;
    return this.http.get<BooksAPI>(url, { params: params }).pipe(
      // tap(_ => this.log(`fetched books id=${id}`)),
      catchError(this.handleError<BooksAPI>(`getBooks id=${id}`))
    );
  }

  // postFront copy books to a version with encoded pointers and post to the back
  postFront(books: Books, GONG__StackPath: string): Observable<BooksAPI> {
    let booksAPI = new BooksAPI
    CopyBooksToBooksAPI(books, booksAPI)
    const id = typeof booksAPI === 'number' ? booksAPI : booksAPI.ID
    const url = `${this.bookssUrl}/${id}`;
    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.post<BooksAPI>(url, booksAPI, httpOptions).pipe(
      tap(_ => {
      }),
      catchError(this.handleError<BooksAPI>('postBooks'))
    );
  }
  
  /** POST: add a new books to the server */
  post(booksdb: BooksAPI, GONG__StackPath: string, frontRepo: FrontRepo): Observable<BooksAPI> {
    return this.postBooks(booksdb, GONG__StackPath, frontRepo)
  }
  postBooks(booksdb: BooksAPI, GONG__StackPath: string, frontRepo: FrontRepo): Observable<BooksAPI> {

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.post<BooksAPI>(this.bookssUrl, booksdb, httpOptions).pipe(
      tap(_ => {
        // this.log(`posted booksdb id=${booksdb.ID}`)
      }),
      catchError(this.handleError<BooksAPI>('postBooks'))
    );
  }

  /** DELETE: delete the booksdb from the server */
  delete(booksdb: BooksAPI | number, GONG__StackPath: string): Observable<BooksAPI> {
    return this.deleteBooks(booksdb, GONG__StackPath)
  }
  deleteBooks(booksdb: BooksAPI | number, GONG__StackPath: string): Observable<BooksAPI> {
    const id = typeof booksdb === 'number' ? booksdb : booksdb.ID;
    const url = `${this.bookssUrl}/${id}`;

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.delete<BooksAPI>(url, httpOptions).pipe(
      tap(_ => this.log(`deleted booksdb id=${id}`)),
      catchError(this.handleError<BooksAPI>('deleteBooks'))
    );
  }

  // updateFront copy books to a version with encoded pointers and update to the back
  updateFront(books: Books, GONG__StackPath: string): Observable<BooksAPI> {
    let booksAPI = new BooksAPI
    CopyBooksToBooksAPI(books, booksAPI)
    const id = typeof booksAPI === 'number' ? booksAPI : booksAPI.ID
    const url = `${this.bookssUrl}/${id}`;
    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.put<BooksAPI>(url, booksAPI, httpOptions).pipe(
      tap(_ => {
      }),
      catchError(this.handleError<BooksAPI>('updateBooks'))
    );
  }

  /** PUT: update the booksdb on the server */
  update(booksdb: BooksAPI, GONG__StackPath: string, frontRepo: FrontRepo): Observable<BooksAPI> {
    return this.updateBooks(booksdb, GONG__StackPath, frontRepo)
  }
  updateBooks(booksdb: BooksAPI, GONG__StackPath: string, frontRepo: FrontRepo): Observable<BooksAPI> {
    const id = typeof booksdb === 'number' ? booksdb : booksdb.ID;
    const url = `${this.bookssUrl}/${id}`;


    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.put<BooksAPI>(url, booksdb, httpOptions).pipe(
      tap(_ => {
        // this.log(`updated booksdb id=${booksdb.ID}`)
      }),
      catchError(this.handleError<BooksAPI>('updateBooks'))
    );
  }

  /**
   * Handle Http operation that failed.
   * Let the app continue.
   * @param operation - name of the operation that failed
   * @param result - optional value to return as the observable result
   */
  private handleError<T>(operation = 'operation in BooksService', result?: T) {
    return (error: any): Observable<T> => {

      // TODO: send the error to remote logging infrastructure
      console.error("BooksService" + error); // log to console instead

      // TODO: better job of transforming error for user consumption
      this.log(`${operation} failed: ${error.message}`);

      // Let the app keep running by returning an empty result.
      return of(result as T);
    };
  }

  private log(message: string) {
    console.log(message)
  }
}
