// generated by ng_file_service_ts
import { Injectable, Component, Inject } from '@angular/core';
import { HttpClientModule, HttpParams } from '@angular/common/http';
import { HttpClient, HttpHeaders } from '@angular/common/http';
import { DOCUMENT, Location } from '@angular/common'

/*
 * Behavior subject
 */
import { BehaviorSubject } from 'rxjs'
import { Observable, of } from 'rxjs'
import { catchError, map, tap } from 'rxjs/operators'

import { DocumentationAPI } from './documentation-api'
import { Documentation, CopyDocumentationToDocumentationAPI } from './documentation'

import { FrontRepo, FrontRepoService } from './front-repo.service';

// insertion point for imports

@Injectable({
  providedIn: 'root'
})
export class DocumentationService {

  // Kamar Raïmo: Adding a way to communicate between components that share information
  // so that they are notified of a change.
  DocumentationServiceChanged: BehaviorSubject<string> = new BehaviorSubject("");

  private documentationsUrl: string

  constructor(
    private http: HttpClient,
    @Inject(DOCUMENT) private document: Document
  ) {
    // path to the service share the same origin with the path to the document
    // get the origin in the URL to the document
    let origin = this.document.location.origin

    // if debugging with ng, replace 4200 with 8080
    origin = origin.replace("4200", "8080")

    // compute path to the service
    this.documentationsUrl = origin + '/api/github.com/fullstack-lang/gongxsd/alt/go/v1/documentations';
  }

  /** GET documentations from the server */
  // gets is more robust to refactoring
  gets(GONG__StackPath: string, frontRepo: FrontRepo): Observable<DocumentationAPI[]> {
    return this.getDocumentations(GONG__StackPath, frontRepo)
  }
  getDocumentations(GONG__StackPath: string, frontRepo: FrontRepo): Observable<DocumentationAPI[]> {

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)

    return this.http.get<DocumentationAPI[]>(this.documentationsUrl, { params: params })
      .pipe(
        tap(),
        catchError(this.handleError<DocumentationAPI[]>('getDocumentations', []))
      );
  }

  /** GET documentation by id. Will 404 if id not found */
  // more robust API to refactoring
  get(id: number, GONG__StackPath: string, frontRepo: FrontRepo): Observable<DocumentationAPI> {
    return this.getDocumentation(id, GONG__StackPath, frontRepo)
  }
  getDocumentation(id: number, GONG__StackPath: string, frontRepo: FrontRepo): Observable<DocumentationAPI> {

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)

    const url = `${this.documentationsUrl}/${id}`;
    return this.http.get<DocumentationAPI>(url, { params: params }).pipe(
      // tap(_ => this.log(`fetched documentation id=${id}`)),
      catchError(this.handleError<DocumentationAPI>(`getDocumentation id=${id}`))
    );
  }

  // postFront copy documentation to a version with encoded pointers and post to the back
  postFront(documentation: Documentation, GONG__StackPath: string): Observable<DocumentationAPI> {
    let documentationAPI = new DocumentationAPI
    CopyDocumentationToDocumentationAPI(documentation, documentationAPI)
    const id = typeof documentationAPI === 'number' ? documentationAPI : documentationAPI.ID
    const url = `${this.documentationsUrl}/${id}`;
    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.post<DocumentationAPI>(url, documentationAPI, httpOptions).pipe(
      tap(_ => {
      }),
      catchError(this.handleError<DocumentationAPI>('postDocumentation'))
    );
  }
  
  /** POST: add a new documentation to the server */
  post(documentationdb: DocumentationAPI, GONG__StackPath: string, frontRepo: FrontRepo): Observable<DocumentationAPI> {
    return this.postDocumentation(documentationdb, GONG__StackPath, frontRepo)
  }
  postDocumentation(documentationdb: DocumentationAPI, GONG__StackPath: string, frontRepo: FrontRepo): Observable<DocumentationAPI> {

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.post<DocumentationAPI>(this.documentationsUrl, documentationdb, httpOptions).pipe(
      tap(_ => {
        // this.log(`posted documentationdb id=${documentationdb.ID}`)
      }),
      catchError(this.handleError<DocumentationAPI>('postDocumentation'))
    );
  }

  /** DELETE: delete the documentationdb from the server */
  delete(documentationdb: DocumentationAPI | number, GONG__StackPath: string): Observable<DocumentationAPI> {
    return this.deleteDocumentation(documentationdb, GONG__StackPath)
  }
  deleteDocumentation(documentationdb: DocumentationAPI | number, GONG__StackPath: string): Observable<DocumentationAPI> {
    const id = typeof documentationdb === 'number' ? documentationdb : documentationdb.ID;
    const url = `${this.documentationsUrl}/${id}`;

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.delete<DocumentationAPI>(url, httpOptions).pipe(
      tap(_ => this.log(`deleted documentationdb id=${id}`)),
      catchError(this.handleError<DocumentationAPI>('deleteDocumentation'))
    );
  }

  // updateFront copy documentation to a version with encoded pointers and update to the back
  updateFront(documentation: Documentation, GONG__StackPath: string): Observable<DocumentationAPI> {
    let documentationAPI = new DocumentationAPI
    CopyDocumentationToDocumentationAPI(documentation, documentationAPI)
    const id = typeof documentationAPI === 'number' ? documentationAPI : documentationAPI.ID
    const url = `${this.documentationsUrl}/${id}`;
    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.put<DocumentationAPI>(url, documentationAPI, httpOptions).pipe(
      tap(_ => {
      }),
      catchError(this.handleError<DocumentationAPI>('updateDocumentation'))
    );
  }

  /** PUT: update the documentationdb on the server */
  update(documentationdb: DocumentationAPI, GONG__StackPath: string, frontRepo: FrontRepo): Observable<DocumentationAPI> {
    return this.updateDocumentation(documentationdb, GONG__StackPath, frontRepo)
  }
  updateDocumentation(documentationdb: DocumentationAPI, GONG__StackPath: string, frontRepo: FrontRepo): Observable<DocumentationAPI> {
    const id = typeof documentationdb === 'number' ? documentationdb : documentationdb.ID;
    const url = `${this.documentationsUrl}/${id}`;


    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.put<DocumentationAPI>(url, documentationdb, httpOptions).pipe(
      tap(_ => {
        // this.log(`updated documentationdb id=${documentationdb.ID}`)
      }),
      catchError(this.handleError<DocumentationAPI>('updateDocumentation'))
    );
  }

  /**
   * Handle Http operation that failed.
   * Let the app continue.
   * @param operation - name of the operation that failed
   * @param result - optional value to return as the observable result
   */
  private handleError<T>(operation = 'operation in DocumentationService', result?: T) {
    return (error: any): Observable<T> => {

      // TODO: send the error to remote logging infrastructure
      console.error("DocumentationService" + error); // log to console instead

      // TODO: better job of transforming error for user consumption
      this.log(`${operation} failed: ${error.message}`);

      // Let the app keep running by returning an empty result.
      return of(result as T);
    };
  }

  private log(message: string) {
    console.log(message)
  }
}
