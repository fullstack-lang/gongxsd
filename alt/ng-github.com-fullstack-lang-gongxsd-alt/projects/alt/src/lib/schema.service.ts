// generated by ng_file_service_ts
import { Injectable, Component, Inject } from '@angular/core';
import { HttpClientModule, HttpParams } from '@angular/common/http';
import { HttpClient, HttpHeaders } from '@angular/common/http';
import { DOCUMENT, Location } from '@angular/common'

/*
 * Behavior subject
 */
import { BehaviorSubject } from 'rxjs'
import { Observable, of } from 'rxjs'
import { catchError, map, tap } from 'rxjs/operators'

import { SchemaAPI } from './schema-api'
import { Schema, CopySchemaToSchemaAPI } from './schema'

import { FrontRepo, FrontRepoService } from './front-repo.service';

// insertion point for imports
import { AnnotationAPI } from './annotation-api'

@Injectable({
  providedIn: 'root'
})
export class SchemaService {

  // Kamar Raïmo: Adding a way to communicate between components that share information
  // so that they are notified of a change.
  SchemaServiceChanged: BehaviorSubject<string> = new BehaviorSubject("");

  private schemasUrl: string

  constructor(
    private http: HttpClient,
    @Inject(DOCUMENT) private document: Document
  ) {
    // path to the service share the same origin with the path to the document
    // get the origin in the URL to the document
    let origin = this.document.location.origin

    // if debugging with ng, replace 4200 with 8080
    origin = origin.replace("4200", "8080")

    // compute path to the service
    this.schemasUrl = origin + '/api/github.com/fullstack-lang/gongxsd/alt/go/v1/schemas';
  }

  /** GET schemas from the server */
  // gets is more robust to refactoring
  gets(GONG__StackPath: string, frontRepo: FrontRepo): Observable<SchemaAPI[]> {
    return this.getSchemas(GONG__StackPath, frontRepo)
  }
  getSchemas(GONG__StackPath: string, frontRepo: FrontRepo): Observable<SchemaAPI[]> {

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)

    return this.http.get<SchemaAPI[]>(this.schemasUrl, { params: params })
      .pipe(
        tap(),
        catchError(this.handleError<SchemaAPI[]>('getSchemas', []))
      );
  }

  /** GET schema by id. Will 404 if id not found */
  // more robust API to refactoring
  get(id: number, GONG__StackPath: string, frontRepo: FrontRepo): Observable<SchemaAPI> {
    return this.getSchema(id, GONG__StackPath, frontRepo)
  }
  getSchema(id: number, GONG__StackPath: string, frontRepo: FrontRepo): Observable<SchemaAPI> {

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)

    const url = `${this.schemasUrl}/${id}`;
    return this.http.get<SchemaAPI>(url, { params: params }).pipe(
      // tap(_ => this.log(`fetched schema id=${id}`)),
      catchError(this.handleError<SchemaAPI>(`getSchema id=${id}`))
    );
  }

  // postFront copy schema to a version with encoded pointers and post to the back
  postFront(schema: Schema, GONG__StackPath: string): Observable<SchemaAPI> {
    let schemaAPI = new SchemaAPI
    CopySchemaToSchemaAPI(schema, schemaAPI)
    const id = typeof schemaAPI === 'number' ? schemaAPI : schemaAPI.ID
    const url = `${this.schemasUrl}/${id}`;
    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.post<SchemaAPI>(url, schemaAPI, httpOptions).pipe(
      tap(_ => {
      }),
      catchError(this.handleError<SchemaAPI>('postSchema'))
    );
  }
  
  /** POST: add a new schema to the server */
  post(schemadb: SchemaAPI, GONG__StackPath: string, frontRepo: FrontRepo): Observable<SchemaAPI> {
    return this.postSchema(schemadb, GONG__StackPath, frontRepo)
  }
  postSchema(schemadb: SchemaAPI, GONG__StackPath: string, frontRepo: FrontRepo): Observable<SchemaAPI> {

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.post<SchemaAPI>(this.schemasUrl, schemadb, httpOptions).pipe(
      tap(_ => {
        // this.log(`posted schemadb id=${schemadb.ID}`)
      }),
      catchError(this.handleError<SchemaAPI>('postSchema'))
    );
  }

  /** DELETE: delete the schemadb from the server */
  delete(schemadb: SchemaAPI | number, GONG__StackPath: string): Observable<SchemaAPI> {
    return this.deleteSchema(schemadb, GONG__StackPath)
  }
  deleteSchema(schemadb: SchemaAPI | number, GONG__StackPath: string): Observable<SchemaAPI> {
    const id = typeof schemadb === 'number' ? schemadb : schemadb.ID;
    const url = `${this.schemasUrl}/${id}`;

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.delete<SchemaAPI>(url, httpOptions).pipe(
      tap(_ => this.log(`deleted schemadb id=${id}`)),
      catchError(this.handleError<SchemaAPI>('deleteSchema'))
    );
  }

  // updateFront copy schema to a version with encoded pointers and update to the back
  updateFront(schema: Schema, GONG__StackPath: string): Observable<SchemaAPI> {
    let schemaAPI = new SchemaAPI
    CopySchemaToSchemaAPI(schema, schemaAPI)
    const id = typeof schemaAPI === 'number' ? schemaAPI : schemaAPI.ID
    const url = `${this.schemasUrl}/${id}`;
    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.put<SchemaAPI>(url, schemaAPI, httpOptions).pipe(
      tap(_ => {
      }),
      catchError(this.handleError<SchemaAPI>('updateSchema'))
    );
  }

  /** PUT: update the schemadb on the server */
  update(schemadb: SchemaAPI, GONG__StackPath: string, frontRepo: FrontRepo): Observable<SchemaAPI> {
    return this.updateSchema(schemadb, GONG__StackPath, frontRepo)
  }
  updateSchema(schemadb: SchemaAPI, GONG__StackPath: string, frontRepo: FrontRepo): Observable<SchemaAPI> {
    const id = typeof schemadb === 'number' ? schemadb : schemadb.ID;
    const url = `${this.schemasUrl}/${id}`;


    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.put<SchemaAPI>(url, schemadb, httpOptions).pipe(
      tap(_ => {
        // this.log(`updated schemadb id=${schemadb.ID}`)
      }),
      catchError(this.handleError<SchemaAPI>('updateSchema'))
    );
  }

  /**
   * Handle Http operation that failed.
   * Let the app continue.
   * @param operation - name of the operation that failed
   * @param result - optional value to return as the observable result
   */
  private handleError<T>(operation = 'operation in SchemaService', result?: T) {
    return (error: any): Observable<T> => {

      // TODO: send the error to remote logging infrastructure
      console.error("SchemaService" + error); // log to console instead

      // TODO: better job of transforming error for user consumption
      this.log(`${operation} failed: ${error.message}`);

      // Let the app keep running by returning an empty result.
      return of(result as T);
    };
  }

  private log(message: string) {
    console.log(message)
  }
}
