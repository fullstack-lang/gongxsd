// generated by ng_file_service_ts
import { Injectable, Component, Inject } from '@angular/core';
import { HttpClientModule, HttpParams } from '@angular/common/http';
import { HttpClient, HttpHeaders } from '@angular/common/http';
import { DOCUMENT, Location } from '@angular/common'

/*
 * Behavior subject
 */
import { BehaviorSubject } from 'rxjs'
import { Observable, of } from 'rxjs'
import { catchError, map, tap } from 'rxjs/operators'

import { AnnotationAPI } from './annotation-api'
import { Annotation, CopyAnnotationToAnnotationAPI } from './annotation'

import { FrontRepo, FrontRepoService } from './front-repo.service';

// insertion point for imports
import { DocumentationAPI } from './documentation-api'

@Injectable({
  providedIn: 'root'
})
export class AnnotationService {

  // Kamar Raïmo: Adding a way to communicate between components that share information
  // so that they are notified of a change.
  AnnotationServiceChanged: BehaviorSubject<string> = new BehaviorSubject("");

  private annotationsUrl: string

  constructor(
    private http: HttpClient,
    @Inject(DOCUMENT) private document: Document
  ) {
    // path to the service share the same origin with the path to the document
    // get the origin in the URL to the document
    let origin = this.document.location.origin

    // if debugging with ng, replace 4200 with 8080
    origin = origin.replace("4200", "8080")

    // compute path to the service
    this.annotationsUrl = origin + '/api/github.com/fullstack-lang/gongxsd/alt/go/v1/annotations';
  }

  /** GET annotations from the server */
  // gets is more robust to refactoring
  gets(GONG__StackPath: string, frontRepo: FrontRepo): Observable<AnnotationAPI[]> {
    return this.getAnnotations(GONG__StackPath, frontRepo)
  }
  getAnnotations(GONG__StackPath: string, frontRepo: FrontRepo): Observable<AnnotationAPI[]> {

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)

    return this.http.get<AnnotationAPI[]>(this.annotationsUrl, { params: params })
      .pipe(
        tap(),
        catchError(this.handleError<AnnotationAPI[]>('getAnnotations', []))
      );
  }

  /** GET annotation by id. Will 404 if id not found */
  // more robust API to refactoring
  get(id: number, GONG__StackPath: string, frontRepo: FrontRepo): Observable<AnnotationAPI> {
    return this.getAnnotation(id, GONG__StackPath, frontRepo)
  }
  getAnnotation(id: number, GONG__StackPath: string, frontRepo: FrontRepo): Observable<AnnotationAPI> {

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)

    const url = `${this.annotationsUrl}/${id}`;
    return this.http.get<AnnotationAPI>(url, { params: params }).pipe(
      // tap(_ => this.log(`fetched annotation id=${id}`)),
      catchError(this.handleError<AnnotationAPI>(`getAnnotation id=${id}`))
    );
  }

  // postFront copy annotation to a version with encoded pointers and post to the back
  postFront(annotation: Annotation, GONG__StackPath: string): Observable<AnnotationAPI> {
    let annotationAPI = new AnnotationAPI
    CopyAnnotationToAnnotationAPI(annotation, annotationAPI)
    const id = typeof annotationAPI === 'number' ? annotationAPI : annotationAPI.ID
    const url = `${this.annotationsUrl}/${id}`;
    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.post<AnnotationAPI>(url, annotationAPI, httpOptions).pipe(
      tap(_ => {
      }),
      catchError(this.handleError<AnnotationAPI>('postAnnotation'))
    );
  }
  
  /** POST: add a new annotation to the server */
  post(annotationdb: AnnotationAPI, GONG__StackPath: string, frontRepo: FrontRepo): Observable<AnnotationAPI> {
    return this.postAnnotation(annotationdb, GONG__StackPath, frontRepo)
  }
  postAnnotation(annotationdb: AnnotationAPI, GONG__StackPath: string, frontRepo: FrontRepo): Observable<AnnotationAPI> {

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.post<AnnotationAPI>(this.annotationsUrl, annotationdb, httpOptions).pipe(
      tap(_ => {
        // this.log(`posted annotationdb id=${annotationdb.ID}`)
      }),
      catchError(this.handleError<AnnotationAPI>('postAnnotation'))
    );
  }

  /** DELETE: delete the annotationdb from the server */
  delete(annotationdb: AnnotationAPI | number, GONG__StackPath: string): Observable<AnnotationAPI> {
    return this.deleteAnnotation(annotationdb, GONG__StackPath)
  }
  deleteAnnotation(annotationdb: AnnotationAPI | number, GONG__StackPath: string): Observable<AnnotationAPI> {
    const id = typeof annotationdb === 'number' ? annotationdb : annotationdb.ID;
    const url = `${this.annotationsUrl}/${id}`;

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.delete<AnnotationAPI>(url, httpOptions).pipe(
      tap(_ => this.log(`deleted annotationdb id=${id}`)),
      catchError(this.handleError<AnnotationAPI>('deleteAnnotation'))
    );
  }

  // updateFront copy annotation to a version with encoded pointers and update to the back
  updateFront(annotation: Annotation, GONG__StackPath: string): Observable<AnnotationAPI> {
    let annotationAPI = new AnnotationAPI
    CopyAnnotationToAnnotationAPI(annotation, annotationAPI)
    const id = typeof annotationAPI === 'number' ? annotationAPI : annotationAPI.ID
    const url = `${this.annotationsUrl}/${id}`;
    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.put<AnnotationAPI>(url, annotationAPI, httpOptions).pipe(
      tap(_ => {
      }),
      catchError(this.handleError<AnnotationAPI>('updateAnnotation'))
    );
  }

  /** PUT: update the annotationdb on the server */
  update(annotationdb: AnnotationAPI, GONG__StackPath: string, frontRepo: FrontRepo): Observable<AnnotationAPI> {
    return this.updateAnnotation(annotationdb, GONG__StackPath, frontRepo)
  }
  updateAnnotation(annotationdb: AnnotationAPI, GONG__StackPath: string, frontRepo: FrontRepo): Observable<AnnotationAPI> {
    const id = typeof annotationdb === 'number' ? annotationdb : annotationdb.ID;
    const url = `${this.annotationsUrl}/${id}`;


    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.put<AnnotationAPI>(url, annotationdb, httpOptions).pipe(
      tap(_ => {
        // this.log(`updated annotationdb id=${annotationdb.ID}`)
      }),
      catchError(this.handleError<AnnotationAPI>('updateAnnotation'))
    );
  }

  /**
   * Handle Http operation that failed.
   * Let the app continue.
   * @param operation - name of the operation that failed
   * @param result - optional value to return as the observable result
   */
  private handleError<T>(operation = 'operation in AnnotationService', result?: T) {
    return (error: any): Observable<T> => {

      // TODO: send the error to remote logging infrastructure
      console.error("AnnotationService" + error); // log to console instead

      // TODO: better job of transforming error for user consumption
      this.log(`${operation} failed: ${error.message}`);

      // Let the app keep running by returning an empty result.
      return of(result as T);
    };
  }

  private log(message: string) {
    console.log(message)
  }
}
